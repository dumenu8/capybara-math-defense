import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'capybara.math.battle',
  appName: 'Capybara Math Battle',
  webDir: 'dist/app/browser'
};

export default config;
